/**
 * Central label text + (later) select options.
 * Keep all human-readable strings here to prevent drift across UI steps.
 */

export const OWNER_SURRENDER_FORM_KEY = "owner-surrender";

export const labels = {
  title: "Owner Surrender Form",
  submit: "Submit",
};
